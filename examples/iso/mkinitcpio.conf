# nixarch ISO initramfs. No autodetect: image carries all block/fs
# drivers so the same ISO boots VMs and real hardware.
MODULES=(loop overlay squashfs iso9660 sr_mod cdrom)
BINARIES=()
FILES=()
HOOKS=(base udev keyboard block nixgen)
COMPRESSION="zstd"
